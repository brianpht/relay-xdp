---
# roles/relay-backend/handlers/main.yml

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart relay-backend
  ansible.builtin.systemd:
    name: relay-backend
    state: restarted

