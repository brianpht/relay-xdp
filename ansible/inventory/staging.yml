# Generated by infra/inventory_gen.py from Pulumi stack outputs.
# Do not edit this file manually.
# To regenerate: python infra/inventory_gen.py --stack staging
#
# Groups:
#   staging         - all staging hosts (triggers playbooks/group_vars/staging/ auto-load)
#   relay_servers   - hosts running relay-xdp + kernel module
#   backend_servers - hosts running relay-backend + Redis
#   bench_servers   - hosts running bench_server (game server simulator, staging only)
all:
  children:
    staging:
      children:
        backend_servers:
          hosts:
            backend-staging-1:
              ansible_host: 54.205.185.157
              ansible_user: ubuntu
        bench_servers:
          hosts:
            bench-staging-1:
              ansible_host: 3.211.102.253
              ansible_user: ubuntu
        relay_servers:
          hosts:
            relay-staging-1:
              ansible_host: 44.194.204.240
              ansible_user: ubuntu
              relay_name: relay-staging-1
            relay-staging-2:
              ansible_host: 54.229.160.49
              ansible_user: ubuntu
              relay_name: relay-staging-2
            relay-staging-3:
              ansible_host: 18.136.67.102
              ansible_user: ubuntu
              relay_name: relay-staging-3
            relay-staging-4:
              ansible_host: 3.112.148.59
              ansible_user: ubuntu
              relay_name: relay-staging-4
            relay-staging-5:
              ansible_host: 54.190.195.217
              ansible_user: ubuntu
              relay_name: relay-staging-5
  vars:
    ansible_python_interpreter: /usr/bin/python3
