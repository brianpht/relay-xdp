[package]
name = "relay-xdp-common"
version = "0.1.0"
edition = "2021"

[features]
default = []
# Enable this feature in userspace only (not in eBPF)
user = ["aya"]

[dependencies]
aya = { version = "0.13", optional = true }

[lib]
path = "src/lib.rs"

